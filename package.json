{
  "name": "delife-kitchen",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@netlify/database": "latest",
    "drizzle-kit": "beta",
    "drizzle-orm": "beta"
  }
}
