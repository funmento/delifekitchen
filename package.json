{
  "name": "delife-kitchen",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@netlify/database": "latest",
    "@netlify/identity": "^1.2.0",
    "drizzle-kit": "beta",
    "drizzle-orm": "beta"
  }
}
